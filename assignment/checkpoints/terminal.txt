



# Type your responses on a new line

# 1 - What is the command for listing the files in the current folder (directory)?
    open .

# 2 - What is the command for changing directories?
    cd -- change directory

# 3 - What is the command for going back a folder (to a parent directory)?
    cd ..

# 4 - What is a quick way to create a new folder?
    Use the mkdir -- make directory.

# 5 - What output would you expect when you run the command 'ls' in an empty folder? What about a folder containing files?
    If you use ls in an empty folder it will not list anything in the folder. Compared to a folder with files inside; which will list the files./names.

# 6 - What is a quick way to create a new file?
    Use the command "touch"

# 7 - Run 'git --version' in your terminal. What is the output?
    git version 2.20.1 (Apple Git-117)

# 8 - Why would you run the command 'git status'?
    To see the status of a folder, I would use git_status. The purpose of this command is to allow the individual to see files that are currently
    not being tracked by Git, as well as any new or changed files.

# 9 - What does the command 'git add .' do?
    The git add . command adds new or changed files in your working directory to the Git staging area. This add’s everything to a snapshot via git.
    This is called staging a commit. -- we have to tell git which files we want to be part of this commit.

# 10 - What is the command to push your committed changes to your repo on GitHub?
      The command is git push | This command uploads all local branch commits to the corresponding remote branch.
